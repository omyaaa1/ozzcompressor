{
  "name": "ozzcompressor",
  "version": "1.1.0",
  "description": "Target-size image compression in the browser",
  "type": "module",
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "format": "prettier --write .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.5.0",
    "prettier": "^3.3.3"
  }
}
